@model IEP.Web.Models.Auction.AuctionModel
<link href="~/Content/Site.css" rel="stylesheet" />

      @Model.Name
@*
    <div class="container">
        <div class="row">
            <div class="col-md-3 col-sm-6">
                <span class="thumbnail text-center">
                    <img src="~/Uploads/Images/@Model.Img" style="max-width: 350px; max-height: 150px" />
                    <h4 class="text-danger">
                        @Model.Name </ h4 >
                        <p> Trenutna cena: @Model.CurrentPrice</ p >
                        <p>
                            Trajanje: @model .CurrentTime </ p >

                            <hr class="line">
                            <div class="row">
                                <div class="col-md-6 col-sm-6">
                                    <button type="button" class="btn btn-link">Add to Wishlist</button>
                                </div>
                                <div class="col-md-6 col-sm-6">
                                    <button class="btn btn-danger right"> BID NOW</button>
                                </div>

                            </div>
                </span>
            </div>
        </div>
    </div>
*@
